
# requirements.txt
# Verified against Python 3.12.3 — May 2026
# Pin exact versions so your environment is reproducible across machines.
# To upgrade later: pip install pip-tools && pip-compile requirements.in
 
# ── Core RAG stack ─────────────────────────────────────────────────────────────
langchain==1.3.1
langchain-openai==1.2.1
langchain-chroma==1.1.0
chromadb==1.5.9
openai>=1.30.0
 
# ── Petfinder API client ───────────────────────────────────────────────────────
requests==2.32.3
 
# ── Frontend ───────────────────────────────────────────────────────────────────
streamlit>=1.35.0
 
# ── Utilities ──────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
tiktoken>=0.7.0
 
# ── Optional: swap OpenAI embeddings/LLM for AWS Bedrock (Claude) ─────────────
# boto3>=1.34.0
# langchain-aws>=0.1.0